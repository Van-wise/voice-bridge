# Voice Bridge 后端依赖
# 安装命令: pip install -r requirements.txt

# ─── Web 框架 ───────────────────────────────────────
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pydantic>=2.5.0
pydantic-settings>=2.1.0
python-multipart>=0.0.9    # 文件上传支持
aiofiles>=23.0.0           # 异步文件操作

# ─── 系统交互 ────────────────────────────────────────
pyperclip>=1.8.0           # 剪贴板读写
pyautogui>=0.9.54          # 自动粘贴
pywin32>=306               # Windows API (win32clipboard)

# ─── 美化输出 ────────────────────────────────────────
colorama>=0.4.0            # Windows 控制台颜色支持

# ─── 托盘 ────────────────────────────────────────────
pystray>=0.19.0
pillow>=10.0.0

# ─── 虚拟麦克风 ───────────────────────────────────────
sounddevice>=0.4.0     # 音频流输出到虚拟声卡
numpy>=1.26.0         # 音频数据处理
